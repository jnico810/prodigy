<script type="text/javascript">
  <% if logged_in? %>
    window.currentUser = <%= render("api/users/user.json.jbuilder",
    user: current_user).html_safe %>
  <% end %>

  window.prodigyAssets = {
    playButtonImg: "<%=image_url('play_button.png') %>"
  };
</script>
<main id="root"/>
